import styled from 'styled-components';

export const Text = styled.p`
  font-size: 1.25rem;
  font-weight: 600;
  overflow: hidden;
  overflow-wrap: break-word;
  text-align: center;
  padding: 1rem;
`;
